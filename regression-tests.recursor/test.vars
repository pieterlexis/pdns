BUILDDIR=${BUILDDIR:-..}
PDNS=${PDNS:-$BUILDDIR/pdns/pdns_server}
PDNS2=${PDNS2:-$BUILDDIR/pdns/pdns_server}
PDNSSEC=${PDNSSEC:-$BUILDDIR/pdns/pdnssec}
PDNSCONTROL=${PDNSCONTROL:-$BUILDDIR/pdns/pdns_control}
ZONE2SQL=${ZONE2SQL:-$BUILDDIR/pdns/zone2sql}
SDIG=${SDIG:-$BUILDDIR/pdns/sdig}
SAXFR=${SDIG:-$BUILDDIR/pdns/saxfr}
NSEC3DIG=${SDIG:-$BUILDDIR/pdns/nsec3dig}
RECURSOR=${RECURSOR:-$BUILDDIR/pdns/pdns_recursor}
DNSBULKTEST=${DNSBULKTEST:-$BUILDDIR/pdns/dnsbulktest}

export BUILDDIR PDNS PDNS2 PDNSSEC PDNSCONTROL ZONE2SQL SDIG SAXFR NSEC3DIG RECURSOR DNSBULKTEST
